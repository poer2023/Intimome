{
  "name": "intim-diary",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server/index.js",
    "dev:full": "npx concurrently \"npm run dev\" \"npm run dev:server\"",
    "dev:cf": "npx concurrently \"npm run dev\" \"npx wrangler pages dev ./dist --d1=DB --proxy 3000\"",
    "build": "vite build",
    "preview": "vite preview",
    "pages:dev": "wrangler pages dev ./dist --d1=DB",
    "deploy": "npm run build && wrangler pages deploy ./dist"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "lucide-react": "^0.468.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^6.30.2",
    "recharts": "^2.15.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "concurrently": "^9.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "wrangler": "^3.95.0"
  }
}
